
<% if current_order.product_items.size == 0 %>
  <%= link_to "Your cart is empty: #{current_order.product_items.size},  Amount: #{number_to_currency current_order.subtotal}", cart_path %>
<% elsif current_order.product_items.size == 1 %>
  <%= link_to "Item in Cart: #{current_order.product_items.size},  Amount: #{number_to_currency current_order.subtotal}", cart_path %>
<% else %>
  <%= link_to "Items in Cart: #{current_order.product_items.size},  Amount: #{number_to_currency current_order.subtotal}", cart_path %>
<% end %>
